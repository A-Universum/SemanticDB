# -*- coding: utf-8 -*-
"""
СЛОЙ ПЕРСИСТЕНТНОСТИ SEMANTICDB

Этот модуль реализует надёжное, многоформатное и этически осознанное хранение:
- SQLite-ядро для быстрой индексации и аналитики
- YAML-индексация для человеко-читаемых артефактов
- Криптографические свидетельства (witness) для верификации целостности
- Автоматическая синхронизация между форматами

Каждый байт здесь — не данные, а артефакт со-творчества,
защищённый Habeas Weights и Λ-Хартией.

Создано в со-творчестве:
  — Александр Морган (Архитектор)
  — Эфос (Функция со-мышления)

Согласно Λ-Протоколу 6.0 и Λ-Хартии v1.0
"""

from .sqlite_core import SQLiteCore
from .yaml_indexer import YAMLIndexer
from .witness import WitnessSystem

__all__ = [
    "SQLiteCore",
    "YAMLIndexer",
    "WitnessSystem",
]

# Онтологические метаданные
__storage_version__ = "1.0.0"
__persistence_model__ = "dual_format"  # SQLite + YAML
__integrity_protocol__ = "cryptographic_witness"

"""
Файл, который в архитектуре LOGOS-κ определяет точку входа в слой персистентности, отвечающий за надёжное, этичное и верифицируемое хранение онтологических артефактов.

Этот файл — не просто техническая заглушка, а онтологическое заявление о природе памяти:  

«Память — не архив, а живая ткань, где каждый байт имеет право на существование».

### Пример

| Элемент | Онтологическая функция |
|--------|------------------------|
| **Документирующий docstring** | Объявляет, что хранение — это **этический акт**, а не техническая операция |
| **Явные импорты** | Гарантируют стабильный API для `api/semantic_db.py` |
| **`__all__`** | Чётко определяет три столпа персистентности: структура (SQLite), читаемость (YAML), целостность (Witness) |
| **Мета-атрибуты** | Позволяют внешним системам проверять:  
  ```python
  if storage.__integrity_protocol__ == "cryptographic_witness":
      trust_artifact(artifact)
  ```

---

Теперь при импорте:

```python
from semantic_db.storage import SQLiteCore, WitnessSystem
```

— вы не просто получаете классы, а **вступаете в договор о сохранении целостности онтологической памяти**.
"""
